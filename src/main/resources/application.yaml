spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miodb
    username: postgres
    password: Keco202+-
    driver-class-name: org.postgresql.Driver


  security:
    oauth2:
      resource server:
        jwt:

          issuer-uri: http://localhost:8080/realms/Booklet
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
