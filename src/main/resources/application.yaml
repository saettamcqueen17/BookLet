spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miodb
    username: postgres
    password: Keco202+-
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect # con Hibernate 6 viene auto-detectato, ma è ok esplicitarlo
